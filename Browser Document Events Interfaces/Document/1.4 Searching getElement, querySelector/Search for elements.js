//The table with id="age-table".

document.querySelector('#age-table')

//All label elements inside that table (there should be 3 of them).

document.querySelectorAll('table label')

//The first td in that table (with the word “Age”).

document.querySelector('table td')

//The form with name="search".

document.querySelector('[name="search"]')

//The first input in that form.

document.querySelector('input')

//The last input in that form.

document.querySelectorAll('input')[document.querySelectorAll('input').length -1]